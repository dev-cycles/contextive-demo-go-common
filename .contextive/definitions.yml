contexts:
  - name: Demo
    domainVisionStatement: To demonstrate the use of a central definitions file shared with other repos in the same bounded context via native package management.
    paths:
    # Globs are supported. Multiple paths may be included. If any match the currently open file, the context will be used.
    - "**" # This is the default path glob if the `paths` key is excluded, and will match all files
    terms:
    - name: Sample
      definition: A sample term used to illustrate its use in both a common library repo, and dependent repos.
      examples:
        - Please, may I have a sample?
    - name: Taste
      definition: To check on a sample.
      examples:
        - I really like the taste of this sample! 😍